---
date: "2026-06-18"
title: "Equal Crash Probability"
difficulty: "Hard"
topic: "Probability"
hint: "A 3-engine plane crashes if 2 or more engines fail: P₃ = C(3,2)p²(1−p) + p³. A 5-engine plane crashes if 3 or more fail: P₅ = C(5,3)p³(1−p)² + C(5,4)p⁴(1−p) + p⁵. Set P₃ = P₅ and solve. Factor out p² (p=0 is trivial). The resulting equation in p should factor nicely."
---

A manufacturer of airplane parts makes a certain engine that has a probability p of failing on any given flight. There are two planes: one with 3 engines and one with 5. A plane crashes if more than half its engines fail. For what values of p do the two plane models have the same probability of crashing?
